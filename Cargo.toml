[package]
name = "plug"
version = "0.1.0"
edition = "2024"

[dependencies]
dyn-utils = { version = "0.1.1", default-features = false, features = ["alloc"] }
facet = { version = "0.46.5", default-features = false, features = ["alloc"] }
hazymacros = { version = "0.1.0", path = "../hazymacros" }
paste = "1.0.15"
