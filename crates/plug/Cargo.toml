[package]
name = "plug"
version = "0.1.0"
edition = "2024"

[dependencies]
eyre = "0.6.14"
facet = "0.46.5"
facet-value = { version = "0.46.0", optional = true }
plug-macro = { path = "../plug-macro" }

[features]
dyn = ["dep:facet-value"]
default = ["dyn"]
